<!DOCTYPE html>
<html>
<head>
    <title>Tax Form - Income Entry</title>
    <style>
        body { font-family: Arial, sans-serif; background: #f7f7fa; }
        .container { max-width: 400px; margin: 40px auto; background: #fff; padding: 32px; border-radius: 10px; box-shadow: 0 8px 32px rgba(0,0,0,0.08);}
        h2 { text-align: center; margin-bottom: 24px; }
        .error { color: #b71c1c; background: #fdeaea; padding: 10px; border-radius: 6px; margin-bottom: 16px; }
        label { display: block; margin-bottom: 8px; }
        input[type="number"] { width: 100%; padding: 8px; margin-bottom: 18px; border-radius: 4px; border: 1px solid #ccc; }
        button { width: 100%; padding: 10px; background: #3a3a7c; color: #fff; border: none; border-radius: 6px; font-size: 1rem; cursor: pointer; }
        button:hover { background: #2a2a5c; }
    </style>
</head>
<body>
    <div class="container">
        <h2>Enter Your Income</h2>
        <% if (error) { %>
            <div class="error"><%= error %></div>
        <% } %>
        <form method="POST" action="/calculate">
            <label for="income1">Income Source 1:</label>
            <input type="number" id="income1" name="income1" min="0" step="any" required value="<%= values.income1 %>">

            <label for="income2">Income Source 2:</label>
            <input type="number" id="income2" name="income2" min="0" step="any" required value="<%= values.income2 %>">

            <button type="submit">Calculate Total Income</button>
        </form>
    </div>
</body>
</html>